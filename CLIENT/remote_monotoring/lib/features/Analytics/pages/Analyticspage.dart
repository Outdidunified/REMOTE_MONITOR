import 'package:flutter/material.dart';

class Analyticspage extends StatelessWidget {
  const Analyticspage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Analytivspage'),
      ),
    );
  }
}
